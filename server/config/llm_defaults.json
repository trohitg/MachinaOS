{
  "version": "2026.07.25",
  "last_updated": "2026-07-25",
  "_description": "LLM provider defaults. popular_models drives the ordered global selector and offline fallback independently of token constraints. POLICY (2026-07-23): popular_models lists only models with >= 1M-token context windows — providers whose models are all below 1M carry an empty list and contribute nothing to the global selector (their models stay reachable via the live per-provider fetch_models dropdown). base_url directly configures the nine OpenAI-compatible native providers; dedicated OpenAI, Anthropic, and Gemini providers use SDK defaults, while OpenRouter has its own gateway default. Runtime proxy credentials can override supported endpoints. Model specs are enriched by model_registry.json (OpenRouter cache).",

  "agent": {
    "_description": "Agent execution defaults. recursion_limit and compaction.ratio are sourced from the environment (AGENT_RECURSION_LIMIT, COMPACTION_RATIO via core.config.Settings) — these JSON values are last-resort fallbacks when the env vars aren't loaded (e.g. one-off CLI scripts that bypass Settings). default_temperature stays JSON-resident since it has no env override yet.",
    "recursion_limit": 200,
    "default_temperature": 0.7,
    "compaction": {
      "ratio": 0.8
    }
  },

  "providers": {
    "openai": {
      "default_model": "gpt-5.6-sol",
      "popular_models": ["gpt-5.6-sol", "gpt-5.6-sol-pro", "gpt-5.6-terra", "gpt-5.6-terra-pro", "gpt-5.6-luna", "gpt-5.6-luna-pro", "gpt-5.5", "gpt-5.4", "gpt-4.1"],
      "detection_patterns": ["gpt", "openai", "o1", "o3", "o4"],
      "base_url": "https://api.openai.com/v1",
      "models_endpoint": "https://api.openai.com/v1/models",
      "api_key_param": "openai_api_key",
      "max_tokens_param": "max_tokens",
      "_models_note": "GPT-5.6 family (sol/terra/luna) GA 2026-07-09. gpt-5.5 / gpt-5.4 remain GA as legacy tiers. Dropped: gpt-5.2 (new requests blocked 2026-06-30), o3 (API shutdown 2026-12-11), o4-mini (API shutdown 2026-10-23) — they fall back to _default constraints if still selected before shutdown.",
      "max_output_tokens": {
        "gpt-5.6-sol": 128000,
        "gpt-5.6-sol-pro": 128000,
        "gpt-5.6-terra": 128000,
        "gpt-5.6-terra-pro": 128000,
        "gpt-5.6-luna": 128000,
        "gpt-5.6-luna-pro": 128000,
        "gpt-5.5": 128000,
        "gpt-5.4": 128000,
        "gpt-4.1": 32768,
        "_default": 16384
      },
      "context_length": {
        "gpt-5.6-sol": 1050000,
        "gpt-5.6-sol-pro": 1050000,
        "gpt-5.6-terra": 1050000,
        "gpt-5.6-terra-pro": 1050000,
        "gpt-5.6-luna": 1050000,
        "gpt-5.6-luna-pro": 1050000,
        "gpt-5.5": 1050000,
        "gpt-5.4": 1050000,
        "gpt-4.1": 1047576,
        "_default": 128000
      },
      "reasoning_models": ["o4-mini", "o3"],
      "thinking_models": ["gpt-5"],
      "thinking_type": "effort",
      "temperature_range": [0.0, 2.0]
    },
    "anthropic": {
      "default_model": "claude-opus-5",
      "popular_models": ["claude-opus-5", "claude-fable-5", "claude-sonnet-5", "claude-opus-4-8", "claude-opus-4-7", "claude-sonnet-4-6"],
      "_models_note": "claude-opus-5 (released 2026-07-24) is the new Opus flagship and default: near-Fable-5 intelligence at Opus pricing ($5/$25 — half of Fable 5), 1M context, 128K max output. The claude-opus-5-fast variant (2x pricing) is deliberately not curated here; select it via the live dropdown if needed.",
      "detection_patterns": ["claude", "anthropic"],
      "base_url": "https://api.anthropic.com/v1",
      "models_endpoint": "https://api.anthropic.com/v1/models",
      "api_key_param": "anthropic_api_key",
      "max_tokens_param": "max_tokens",
      "max_output_tokens": {
        "claude-opus-5": 128000,
        "claude-fable-5": 128000,
        "claude-opus-4-8": 128000,
        "claude-opus-4-7": 128000,
        "claude-sonnet-5": 128000,
        "claude-sonnet-4-6": 128000,
        "claude-haiku-4-5": 64000,
        "_default": 8192
      },
      "context_length": {
        "claude-opus-5": 1048576,
        "claude-fable-5": 1048576,
        "claude-opus-4-8": 1048576,
        "claude-opus-4-7": 1048576,
        "claude-sonnet-5": 1048576,
        "claude-sonnet-4-6": 1048576,
        "claude-haiku-4-5": 200000,
        "_default": 200000
      },
      "thinking_type": "budget",
      "_capability_note": "Two request-shape breaks landed together in the Claude 4.7+ generations: `temperature`/`top_p`/`top_k` are rejected (400 `temperature is deprecated for this model`), and the `{type: enabled, budget_tokens: N}` thinking form is rejected in favour of `{type: adaptive}`. Both lists are prefix-matched by AnthropicProvider._model_policy. claude-opus-4-6 / claude-sonnet-4-6 are deliberately absent: adaptive is recommended there but budget_tokens still works, so they keep the legacy shape until Anthropic removes it.",
      "adaptive_thinking_models": [
        "claude-fable-5",
        "claude-mythos-5",
        "claude-opus-5",
        "claude-opus-4-8",
        "claude-opus-4-7",
        "claude-sonnet-5"
      ],
      "sampling_params_removed": [
        "claude-fable-5",
        "claude-mythos-5",
        "claude-opus-5",
        "claude-opus-4-8",
        "claude-opus-4-7",
        "claude-sonnet-5"
      ],
      "temperature_range": [0.0, 1.0]
    },
    "gemini": {
      "default_model": "gemini-3.6-flash",
      "popular_models": ["gemini-3.6-flash", "gemini-3.5-flash", "gemini-3.5-flash-lite", "gemini-3.1-pro-preview", "gemini-3.1-flash-lite", "gemini-3-flash-preview", "gemini-2.5-pro", "gemini-2.5-flash", "gemini-2.5-flash-lite"],
      "detection_patterns": ["gemini", "google", "gemma"],
      "_detection_patterns_note": "'gemma' makes is_model_valid_for_provider accept the API-served Gemma models (gemma-4-*-it) on gemini nodes — without it the four agent-path call sites silently swap gemma to default_model. Cerebras keeps its own 'gemma-4' pattern for its gemma-4-31b (no -it suffix); is_model_valid_for_provider is per-provider so both coexist. Only the runtime-unused detect_provider_from_model classifier prefers gemini for gemma-* names (dict order).",
      "base_url": "https://generativelanguage.googleapis.com/v1beta",
      "models_endpoint": "https://generativelanguage.googleapis.com/v1beta/models",
      "api_key_param": "google_api_key",
      "max_tokens_param": "max_output_tokens",
      "_models_note": "Real (concrete) model names only — no -latest rolling aliases. The curated list (max_output_tokens keys) serves BOTH the Gemini Developer API (AIza keys) and Vertex / Agent Platform (AQ. keys); gemini-* entries live-verified on both endpoints (2026-06-12). Vertex 404s on -latest aliases and its models.list rejects API keys, which is why the dropdown is curated instead of live-fetched. Gemma exception: gemma-4-31b-it + gemma-4-26b-a4b-it are served ONLY on the Developer API (free tier, ai.google.dev/gemma/docs/core/gemma_on_gemini_api, verified 2026-07-11) — Vertex keys must not see them, hence vertex_incompatible_models below. Gemma 4 supports systemInstruction and function calling natively (unlike the gemma-3 era); max output is undocumented, so the curated 8192 is a conservative fallback (registry cache wins when present).",
      "max_output_tokens": {
        "gemini-3.6-flash": 65536,
        "gemini-3.5-flash": 65536,
        "gemini-3.5-flash-lite": 65536,
        "gemini-3.1-pro-preview": 65536,
        "gemini-3.1-flash-lite": 65536,
        "gemini-3-flash-preview": 65536,
        "gemini-2.5-pro": 65536,
        "gemini-2.5-flash": 65535,
        "gemini-2.5-flash-lite": 65535,
        "gemma-4-31b-it": 8192,
        "gemma-4-26b-a4b-it": 8192,
        "_default": 8192
      },
      "context_length": {
        "gemini-3.6-flash": 1048576,
        "gemini-3.5-flash": 1048576,
        "gemini-3.5-flash-lite": 1048576,
        "gemini-3.1-pro-preview": 1048576,
        "gemini-3.1-flash-lite": 1048576,
        "gemini-3-flash-preview": 1048576,
        "gemini-2.5-pro": 1048576,
        "gemini-2.5-flash": 1048576,
        "gemini-2.5-flash-lite": 1048576,
        "gemma-4-31b-it": 262144,
        "gemma-4-26b-a4b-it": 262144,
        "_default": 1048576
      },
      "_vertex_incompatible_models_note": "Curated models that only exist on the Gemini Developer API, filtered out of fetch_models when the key is a Vertex / Agent Platform key (GeminiProvider._vertex). Gemma is Model Garden self-deploy on Vertex (the 26B MaaS variant needs a different '-maas' id on a different surface), so the plain ids 404 there.",
      "vertex_incompatible_models": ["gemma-4-31b-it", "gemma-4-26b-a4b-it"],
      "thinking_models": ["gemini-2.5", "gemini-3"],
      "_new_models_note": "gemini-3.6-flash (new default) and gemini-3.5-flash-lite both launched 2026-07-21 with 1M context / 65536 max output; 3.6 Flash is $1.50/$7.50, Flash-Lite $0.30/$2.50. gemini-3.5-pro is still not GA on the public API (enterprise Vertex preview only) so it stays out of the curated list.",
      "thinking_type": "budget",
      "temperature_range": [0.0, 2.0],
      "_incompatible_models_note": "Models that the dropdown should hide because they need a different API endpoint than `generate_content`. antigravity-preview-05-2026 requires the Interactions API; selecting it currently surfaces a `google.genai.errors.APIError`. ChatUnifier reads this list in fetch_models and filters before returning. Drop entries here when the unifier gains routing support for the alternate endpoint.",
      "incompatible_models": ["antigravity-preview-05-2026"]
    },
    "openrouter": {
      "default_model": "anthropic/claude-sonnet-5",
      "popular_models": ["anthropic/claude-sonnet-5", "openai/gpt-5.6-terra", "openai/gpt-5.6-sol", "google/gemini-3.5-flash", "moonshotai/kimi-k3", "x-ai/grok-4.20"],
      "detection_patterns": ["openrouter"],
      "base_url": "https://openrouter.ai/api/v1",
      "models_endpoint": "https://openrouter.ai/api/v1/models",
      "api_key_param": "api_key",
      "max_tokens_param": "max_tokens",
      "extra_headers": {"HTTP-Referer": "http://localhost:5678", "X-Title": "OpenCompany"},
      "max_output_tokens": {
        "_default": 4096
      },
      "context_length": {
        "_default": 128000
      },
      "temperature_range": [0.0, 2.0]
    },
    "groq": {
      "default_model": "openai/gpt-oss-120b",
      "popular_models": [],
      "_popular_note": "Empty per the 1M-context policy — no Groq-served model reaches 1M (all top out at 131072). Models remain reachable via live fetch_models.",
      "detection_patterns": ["groq"],
      "base_url": "https://api.groq.com/openai/v1",
      "models_endpoint": "https://api.groq.com/openai/v1/models",
      "api_key_param": "api_key",
      "max_tokens_param": "max_tokens",
      "_models_note": "Default moved to openai/gpt-oss-120b (production route). Groq announced deprecation of llama-3.3-70b-versatile and llama-3.1-8b-instant on 2026-06-17 (console.groq.com/docs/deprecations); kept in the list while still served — they fall back to _default constraints after removal.",
      "max_output_tokens": {
        "openai/gpt-oss-120b": 65536,
        "openai/gpt-oss-20b": 65536,
        "llama-3.3-70b-versatile": 32768,
        "llama-3.1-8b-instant": 131072,
        "qwen/qwen3-32b": 40960,
        "_default": 8192
      },
      "context_length": {
        "openai/gpt-oss-120b": 131072,
        "openai/gpt-oss-20b": 131072,
        "llama-3.3-70b-versatile": 131072,
        "llama-3.1-8b-instant": 131072,
        "qwen/qwen3-32b": 131072,
        "_default": 131072
      },
      "thinking_models": ["qwen3"],
      "thinking_type": "format",
      "temperature_range": [0.0, 2.0]
    },
    "cerebras": {
      "default_model": "gpt-oss-120b",
      "popular_models": [],
      "_popular_note": "Empty per the 1M-context policy — no Cerebras model reaches 1M (all 131072). Models remain reachable via live fetch_models.",
      "detection_patterns": ["cerebras", "gpt-oss", "zai-glm", "gemma-4"],
      "base_url": "https://api.cerebras.ai/v1",
      "models_endpoint": "https://api.cerebras.ai/v1/models",
      "api_key_param": "api_key",
      "max_tokens_param": "max_tokens",
      "_models_note": "Per inference-docs.cerebras.ai 2026-07-11: gpt-oss-120b is the only production model (paid tier 131k context / 40k max completion); zai-glm-4.7 and gemma-4-31b are preview. qwen-3-235b* variants are shut down (migration target: gpt-oss-120b).",
      "max_output_tokens": {
        "gpt-oss-120b": 40960,
        "zai-glm-4.7": 40960,
        "gemma-4-31b": 40960,
        "_default": 8192
      },
      "context_length": {
        "gpt-oss-120b": 131072,
        "zai-glm-4.7": 131072,
        "gemma-4-31b": 131072,
        "_default": 131072
      },
      "thinking_models": ["zai-glm-4.7"],
      "thinking_type": "budget",
      "temperature_range": [0.0, 1.5]
    },
    "xai": {
      "default_model": "grok-4.20",
      "popular_models": ["grok-4.20", "grok-4.20-multi-agent", "grok-4.3"],
      "_popular_note": "grok-4.5 (flagship, released 2026-07-08) and grok-3 excluded per the 1M-context policy (500K / 131072); reachable via live fetch_models. Default stays grok-4.20 (2M) so the default is selectable from the global list.",
      "detection_patterns": ["grok", "xai"],
      "base_url": "https://api.x.ai/v1",
      "models_endpoint": "https://api.x.ai/v1/models",
      "api_key_param": "api_key",
      "max_tokens_param": "max_tokens",
      "_models_note": "grok-4.5 (released 2026-07-08): 500K context, configurable reasoning effort (default high), $2/$6 per MTok. Output cap undocumented by xAI — curated 131072 matches the rest of the grok family; registry cache wins when present.",
      "max_output_tokens": {
        "grok-4.5": 131072,
        "grok-4.20": 131072,
        "grok-4.3": 131072,
        "grok-3": 131072,
        "_default": 8192
      },
      "context_length": {
        "grok-4.20": 2000000,
        "grok-4.20-multi-agent": 2000000,
        "grok-4.5": 500000,
        "grok-4.3": 1000000,
        "grok-3": 131072,
        "_default": 131072
      },
      "temperature_range": [0.0, 2.0]
    },
    "deepseek": {
      "default_model": "deepseek-v4-flash",
      "popular_models": ["deepseek-v4-flash", "deepseek-v4-pro"],
      "_popular_note": "deepseek-chat / deepseek-reasoner dropped from the global list per the 1M-context policy (131072); reachable via live fetch_models.",
      "detection_patterns": ["deepseek"],
      "base_url": "https://api.deepseek.com",
      "models_endpoint": "https://api.deepseek.com/models",
      "api_key_param": "api_key",
      "max_tokens_param": "max_tokens",
      "supported_params": ["temperature", "max_tokens", "top_p", "frequency_penalty", "presence_penalty", "stop", "seed", "tools", "tool_choice", "response_format"],
      "max_output_tokens": {
        "deepseek-v4-flash": 65536,
        "deepseek-v4-pro": 65536,
        "deepseek-chat": 8192,
        "deepseek-reasoner": 65536,
        "_default": 8192
      },
      "context_length": {
        "deepseek-v4-flash": 1048576,
        "deepseek-v4-pro": 1048576,
        "deepseek-chat": 131072,
        "deepseek-reasoner": 131072,
        "_default": 131072
      },
      "reasoning_models": ["deepseek-reasoner"],
      "temperature_range": [0.0, 2.0]
    },
    "kimi": {
      "default_model": "kimi-k3",
      "popular_models": ["kimi-k3"],
      "_popular_note": "Only kimi-k3 meets the 1M-context policy (k2.x are 262144); k2.x remain reachable via live fetch_models.",
      "_models_note": "kimi-k3 GA (platform.kimi.ai): 2.8T params, 1M context, vision + tool calling + structured outputs, $3/$15 per MTok ($0.30 cache-hit). Default max_completion_tokens 131072 (configurable up to the full window). k2.x kept as cheaper tiers.",
      "detection_patterns": ["kimi", "moonshot"],
      "base_url": "https://api.moonshot.ai/v1",
      "models_endpoint": "https://api.moonshot.ai/v1/models",
      "api_key_param": "api_key",
      "max_tokens_param": "max_tokens",
      "supported_params": ["temperature", "max_tokens", "top_p", "frequency_penalty", "presence_penalty", "stop", "seed", "tools", "tool_choice", "response_format"],
      "thinking_default_on": ["kimi-k2.6", "kimi-k2.5", "kimi-k2.7-code"],
      "fixed_temperature": {"kimi-k2.6": 0.6, "kimi-k2.5": 0.6, "kimi-k2.7-code": 0.6},
      "max_output_tokens": {
        "kimi-k3": 131072,
        "kimi-k2.6": 32768,
        "kimi-k2.5": 98304,
        "kimi-k2.7-code": 98304,
        "_default": 8192
      },
      "context_length": {
        "kimi-k3": 1048576,
        "kimi-k2.6": 262144,
        "kimi-k2.5": 262144,
        "kimi-k2.7-code": 262144,
        "_default": 262144
      },
      "temperature_range": [0.0, 1.0]
    },
    "mistral": {
      "default_model": "mistral-large-latest",
      "popular_models": [],
      "_popular_note": "Empty per the 1M-context policy — Mistral tops out at 262144 (large/medium). Models remain reachable via live fetch_models.",
      "detection_patterns": ["mistral", "codestral"],
      "base_url": "https://api.mistral.ai/v1",
      "models_endpoint": "https://api.mistral.ai/v1/models",
      "api_key_param": "api_key",
      "max_tokens_param": "max_tokens",
      "supported_params": ["temperature", "max_tokens", "top_p", "frequency_penalty", "presence_penalty", "stop", "seed", "tools", "tool_choice", "response_format"],
      "max_output_tokens": {
        "mistral-large-latest": 131072,
        "mistral-medium-latest": 131072,
        "mistral-small-latest": 32768,
        "codestral-latest": 131072,
        "_default": 8192
      },
      "context_length": {
        "mistral-large-latest": 262144,
        "mistral-medium-latest": 262144,
        "mistral-small-latest": 32768,
        "codestral-latest": 256000,
        "_default": 131072
      },
      "temperature_range": [0.0, 1.5]
    },
    "sarvam": {
      "default_model": "sarvam-105b",
      "popular_models": [],
      "_popular_note": "Empty per the 1M-context policy — sarvam-105b tops out at 131072. Both models stay reachable via the per-node dropdown (fetch_models returns the curated list) and via the global selector, which falls back to the max_output_tokens keys when popular_models is empty.",
      "_models_note": "sarvam-105b (128K ctx) and sarvam-30b (64K ctx), OpenAI-compatible at api.sarvam.ai/v1. sarvam-m is deprecated/removed and deliberately absent. Reasoning is ON by default (reasoning_effort defaults to 'medium' server-side) and the trace comes back as message.reasoning_content, which OpenAIProvider._normalize already reads.",
      "detection_patterns": ["sarvam"],
      "base_url": "https://api.sarvam.ai/v1",
      "models_endpoint": "",
      "supports_model_listing": false,
      "_models_listing_note": "Sarvam ships NO model-list endpoint — verified against https://docs.sarvam.ai/openapi.json (25 paths, none for model listing). OpenAIProvider.fetch_models reads this flag and swaps models.list() for a 1-token chat probe plus the curated list. Without it the 404 becomes an openai.OpenAIError, ChatUnifier turns that into NodeUserError, and AIService.fetch_models re-raises before its curated fallback — breaking both credential validation and the model dropdown.",
      "api_key_param": "api_key",
      "max_tokens_param": "max_tokens",
      "supported_params": ["temperature", "max_tokens", "top_p", "frequency_penalty", "presence_penalty", "stop", "seed", "n", "tools", "tool_choice", "response_format", "reasoning_effort"],
      "max_output_tokens": {
        "sarvam-105b": 4096,
        "sarvam-30b": 4096,
        "_default": 4096
      },
      "_max_output_note": "SUBSCRIPTION-TIER CAP, not a model limit — this is the one provider here whose output ceiling depends on the ACCOUNT rather than the model. Sarvam documents Starter 4096 / Pro 16384 / Business 128000 for sarvam-105b and 4096 / 8192 / 64000 for sarvam-30b, and rejects anything above your tier with a 400 ('max_tokens (N) exceeds the maximum allowed ... for your subscription tier'). services/llm/config.py::resolve_max_tokens uses this number BOTH as the default budget when a node leaves max_tokens unset AND as the ceiling it clamps larger user values down to, so a single value has to hold for every account — hence the Starter cap, the only figure that succeeds on all tiers. Sarvam's own API default is 2048, so 4096 is already generous. ON PRO OR BUSINESS: raise these to your tier's ceiling (setting max_tokens on the node alone is NOT enough — resolve_max_tokens clamps it back down to whatever is written here). Do not use the Business figures as a default: they are ~98% of each context window, leaving no room for a prompt.",
      "context_length": {
        "sarvam-105b": 131072,
        "sarvam-30b": 65536,
        "_default": 65536
      },
      "thinking_models": ["sarvam-"],
      "thinking_type": "effort",
      "_thinking_note": "reasoning_models is deliberately empty: listing sarvam there would set temperature_allowed=False and pin temperature to 1.0, but Sarvam accepts temperature alongside reasoning. thinking_default_on is deliberately absent too — it emits Moonshot's proprietary extra_body.thinking={'type':'disabled'}, which Sarvam never documented. With both omitted, thinking off = Sarvam's own default (reasoning on, effort medium) and thinking on = our reasoning_effort.",
      "temperature_range": [0.0, 2.0]
    },

    "llmtr": {
      "_description": "LLMTR (llmtr.com) — Turkish multi-vendor LLM gateway, OpenAI-compatible at llmtr.com/v1. Same SHAPE as openrouter: one key fronts many vendors and model ids are `vendor/model` (openai/gpt-4o, anthropic/claude-sonnet-5, llmtr/trendyol-7b). That naming is why `llmtr` is in the open-world tuple in services/llm/config.py::is_model_valid_for_provider AND in _ROUTER_PROVIDERS in services/model_registry.py — no llmtr model id contains the substring 'llmtr' except its own self-hosted rows, so the cloud-style pattern check would reject every valid model, and the vendor-prefixed ids resolve their real context/output limits out of the OpenRouter cache by stripping the prefix.",
      "default_model": "anthropic/claude-sonnet-5",
      "popular_models": [
        "anthropic/claude-sonnet-5",
        "openai/gpt-5.4",
        "google/gemini-3.6-flash",
        "qwen/qwen3.8-max",
        "deepseek/deepseek-v4-pro",
        "llmtr/gemma-4",
        "llmtr/trendyol-7b"
      ],
      "_popular_note": "Curated cross-vendor spread plus the two Turkey-hosted llmtr rows. Exempt from the >=1M-context global-list policy the same way openrouter's vendor-prefixed ids are: no per-model context_length is declared here because the true windows live in the router cache (config/model_registry.json), which the _ROUTER_PROVIDERS cross-lookup reaches by stripping the vendor prefix.",
      "detection_patterns": ["llmtr"],
      "base_url": "https://llmtr.com/v1",
      "models_endpoint": "https://llmtr.com/v1/models",
      "_models_listing_note": "GET /v1/models is PUBLIC (200 with no key, and 200 with an invalid key) and returns all 229 catalogue rows — chat, embeddings, image, video, rerank. Two consequences, both handled outside this file. (1) The list must be filtered to supported_operations containing CHAT_COMPLETIONS or the chat dropdown fills with voyageai embeddings and recraft image models that 404 on /v1/chat/completions; that filter lives in services/llm/providers/llmtr.py. (2) Because the route ignores the Authorization header, the default _LLMApiKey._probe (which just calls fetch_models) would accept ANY string as a valid key — LLMTRCredential._probe therefore overrides it with a 1-token chat completion, the cheapest call that actually returns 401 'Invalid API key'.",
      "api_key_param": "api_key",
      "max_tokens_param": "max_tokens",
      "routes_vendor_prefixed_models": true,
      "vendor_aliases": {"google": "gemini", "moonshot": "kimi", "x-ai": "xai"},
      "_routing_note": "Turns on the gateway branch in OpenAIProvider._model_policy: the WIRE SHAPE (max_completion_tokens vs max_tokens, whether temperature may be sent) is resolved against the routed VENDOR's block, because those are properties of the model that ultimately serves the call. Verified live: openai/o4-mini and openai/gpt-5-nano both 400 ('The model provider rejected the request') when sent max_tokens + temperature, and both succeed on max_completion_tokens with temperature omitted. vendor_aliases maps LLMTR's prefix spelling onto our provider keys; an unlisted vendor (qwen, zai, minimax, llmtr's own rows, ...) keeps this generic block, which is the right conservative default. THINKING is deliberately NOT vendor-resolved — see thinking_type below.",
      "thinking_type": "effort",
      "_thinking_note": "LLMTR normalizes reasoning across vendors onto the OpenAI-style reasoning_effort parameter — its anthropic/* and google/* rows accept reasoning_effort even though those vendors natively take token budgets, and an extra_body thinking budget is silently ignored. So the gateway's own type is 'effort' regardless of who serves the model, and adopting the vendor's native type would emit proprietary extra_body LLMTR never documented. Support is per-model: unsupported rows answer 400 with details.supportsReasoningToggle=false ('This model does not expose a thinking mode'), which at time of writing includes every openai/* row while anthropic/* and google/* accept it. That 400 only occurs when the user explicitly ticks thinking, and surfacing it is better than silently dropping the setting and reporting reasoning that never ran.",
      "max_output_tokens": {"_default": 4096},
      "context_length": {"_default": 128000},
      "temperature_range": [0.0, 2.0]
    },

    "ollama": {
      "_description": "Local Ollama server (OpenAI-compatible /v1). No `default_model` because the dropdown should reflect the user's actually-pulled models — fetch_models hits the user's saved {provider}_proxy URL and returns whatever Ollama reports; when the server is offline we want an empty list (a 'start your server' cue), not a placeholder name the user almost certainly doesn't have.",
      "detection_patterns": ["ollama"],
      "base_url": "http://localhost:11434/v1",
      "models_endpoint": "http://localhost:11434/v1/models",
      "api_key_param": "api_key",
      "max_tokens_param": "max_tokens",
      "max_output_tokens": {"_default": 4096},
      "context_length": {"_default": 32768},
      "temperature_range": [0.0, 2.0]
    },

    "lmstudio": {
      "_description": "Local LM Studio server (pure OpenAI-compatible). Same no-default-model rule as ollama — the dropdown reflects what the user has loaded in LM Studio, not a fake placeholder.",
      "detection_patterns": ["lmstudio", "lm_studio", "lm-studio"],
      "base_url": "http://localhost:1234/v1",
      "models_endpoint": "http://localhost:1234/v1/models",
      "api_key_param": "api_key",
      "max_tokens_param": "max_tokens",
      "max_output_tokens": {"_default": 4096},
      "context_length": {"_default": 32768},
      "temperature_range": [0.0, 2.0]
    }
  }
}
